apiVersion: v1
kind: Service
metadata:
  name: devops-practice-app
  namespace: default
  labels:
    app: devops-practice-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: devops-practice-app
